//
 var testArray = ["banana","apple", "peach","pear"];

console.log(testArray.length); //array name and number of items in array

//push (add) a new item to the end of the array
testArray.push("carrot","grapes");
console.log(testArray);
testArray[1]="oranges";


//pop (take away) removes last item from array
testArray.pop();
console.log(testArray);